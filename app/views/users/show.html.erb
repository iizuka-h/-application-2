<div class="container-fluid">
  <div class="row　sampleRow">

    <div class="col-sm-4">
      <%= render 'users/profile', user: @user %>
      <%= render 'books/form', book: @book %>
    </div>

<div class="col-sm-8">
   <h2>Books</h2>
   <table class="table">
    <thead>
      <tr>
        <th>Title</th>
        <th>Opinion</th>  
      </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
        <tr>
          <td><%= attachment_image_tag book.user, :profile_image, size: "40x40" , fallback: "no_image.jpg"%></td>
          <td><%= book.title %></td>
          <td><%= book.body %></td>
        </tr>
        <% end %>
    </tbody>
    </table>
 </div>

 </div>
</div>